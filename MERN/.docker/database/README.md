Drop your data import .js into this folder.

Content is inserted by doing the following:

```
db.MyModel.insert({
    name: "Data Entry 01",
    content: { foo: bar },
    created: 0,
    updated: 0
});
```